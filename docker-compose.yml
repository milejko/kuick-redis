#test runner with the Redis server for integration tests

services:
  test-runner:
    depends_on:
      - test-redis
    build:
      args:
        PHP_VERSION: ${PHP_VERSION:-8.5}
    volumes:
      - ./:/var/www/html

  test-redis:
    image: redis:8.0.1
